@model IEnumerable<BPU_Project.Models.Salesorder>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<p>
    @using (Html.BeginForm("Index", "Salesorder", FormMethod.Get))
    {   //the following are search options
        <b>Search Option: </b>@Html.RadioButton("option", "Salesorder") <text>Sales Order Number</text> @Html.RadioButton("option", "Lineitem") <text> Line Item </text>
        @Html.RadioButton("option", "ModuleNo")<text>Module</text> @Html.TextBox("search") <input type="submit" name="submit" value="Search" />
    }
</p>


<table class="table">
    <tr>
        <th>
            Date
        </th>
        <th>
            Module
        </th>
        <th>
            Sales Order
        </th>
        <th>
            Line Item
        </th>
        <th>
            Style
        </th>
        <th>
            Color
        </th>
        <th>
            Size
        </th>
        <th>
            Qty
        </th>
        <th>
            Remarks
        </th>
        @* <th>
            In Module
        </th>
        <th>
            Completed
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Module)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salesorder_No)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Line_Item)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Style)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Color)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Qty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Remark)
        </td>
       @* <td>
            @Html.DisplayFor(modelItem => item.InTheModule)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Completed)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
